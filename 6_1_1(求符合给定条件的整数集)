#include <stdio.h>
/*
求符合给定条件的整数集
给定不超过6的正整数a，考虑从a开始的连续4个数字，输出所有由他们组成的无重复数字的3位数

输入格式:
输入在一行中给出a
输出格式：输出满足条件的3位数，要求从小到大，每行6个整数，整数间以空格分隔，但行末不能有多余空格。
输入样例：
2
输出样例：
234 235 243 245 253 254
324 325 342 345 352 354
423 425 432 435 452 453
523 524 532 534 542 543


思路：
三层嵌套循环遍历：
代码的核心是三层嵌套的 while循环，用于遍历所有可能的三位数组合

外层循环 (i)：i从 a(即2) 开始，每次循环递增1，直到 a+3(即5)。
中层循环 (j)：对于每一个 i的值，j同样从2开始，递增到5。这意味着对于 i的每一个取值，j都要完整地循环一遍。
内层循环 (k)：对于每一对 (i, j)的值，k也从2开始，递增到5。因此，最内层的循环体执行的次数是 4 × 4 × 4 = 64 次

条件判断筛选不重复数字：
在最内层循环中，通过三个 if条件语句筛选出 i, j, k三个数字互不重复的组合
：
if (i != j) {
    if (i != k) {
        if (j != k) {
        }
    }
}
这确保了只有像 234, 235 这样的排列会被输出，而像 223, 333 这样有重复数字的组合会被跳过。

输出与格式控制：
每当找到一个有效的、数字不重复的组合时，计数器 cnt加1，并直接连续打印 i, j, k的值（如 234）。
然后检查 cnt：
如果 cnt等于6，表示已经打印了6个组合，则输出换行符 \n，并将 cnt重置为0，以便开始新的一行。
否则，输出一个空格，用于在同一行内分隔不同的组合。
*/
int main()
{
    int a;
    scanf_s("%d", &a);
    //a = 2;   不能使用a=2给2赋值，使用vscode2026版本会一直是运行中，不会输出，禁用后运行代码输入2可正常输出
    int i, j, k;
    int cnt = 0;
    i = a;
    while (i <= a + 3) {
        j = a;
        while (j <= a + 3) {
            k = a;
            while (k <= a + 3) {
                if (i != j)
                {
                    if (i != k) {
                        if (j != k)
                        {
                            cnt++;
                            printf("%d%d%d", i, j, k);

                            if (cnt == 6)
                            {
                                printf("\n");
                                cnt = 0;
                            }
                            else {
                                printf(" ");
                            }
                        }
                    }
                }k++;
            }j++;
        }i++;
    }
    return 0;
}
