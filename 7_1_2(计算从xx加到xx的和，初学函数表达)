#include <stdio.h>
//先使用void声明函数，之后再使用函数名调用
void sum(int begin, int end){
    int i;
    int sum = 0;
    for (i = begin; i <= end; i++)
    {
        sum += i;
    }
    printf("%d到%d所有数字加起来的和是%d", begin, end, sum);
}
    int begin;
    int end;

    int main(){
        sum(1, 10);
        return 0;
    }


